<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.lin.cloud.mapper.PaymentMapper">

   <!--
    1.parameterType :    传入参数类型
    2.useGeneratedKeys : 生成返回值 一般插入是int
    3.keyProperty      : 主键
   <insert id="create" parameterType="Payment" useGeneratedKeys="true" keyProperty="id">
        insert into payment(serial)  values(#{serial});
    </insert>

  内容映射
    <resultMap id="BaseResultMap" type="com.lin.cloud.pojo.Payment">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <id column="serial" property="serial" jdbcType="VARCHAR"/>
    </resultMap>
    <select id="getPaymentById" parameterType="Long" resultMap="BaseResultMap">
        select * from payment where id=#{id};
    </select>-->

    <insert id="insPayment">
        insert into payment(id,serial)  values(DEFAULT,#{serial});
    </insert>
</mapper>


